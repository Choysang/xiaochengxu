<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androiddingjing">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera2" />
    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidDemo"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.ManageStudent" />
        <activity android:name=".activities.StudentIdActivity" />
        <activity android:name=".activities.TeacherIdActivity" />
        <activity android:name=".activities.ClassIdActivity" />
        <activity android:name=".activities.QueryInfoActivity" />
        <activity
            android:name=".activities.ChangeInfoActivity"
            android:theme="@style/Theme.AndroidDemo.NoActionBar" />
        <activity
            android:name=".activities.GroupActivity"
            android:theme="@style/Theme.AndroidDemo.NoActionBar" />
        <activity
            android:name=".activities.ContentActivity"
            android:theme="@style/Theme.AndroidDemo.NoActionBar" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.AndroidDemo.NoActionBar" />
        <activity
            android:name=".activities.LoginAndRegisterActivity"
            android:theme="@style/Theme.AndroidDemo.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.CameraActivity" />
        <activity android:name=".activities.RecordActivity" />
        <activity android:name=".activities.camera" />
    </application>

</manifest>